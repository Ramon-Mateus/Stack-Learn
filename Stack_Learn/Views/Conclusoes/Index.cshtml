@model IEnumerable<Modelos.Models.Conclusao>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Aluno.CPF)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Concluido)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Aluno.CPF)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Concluido)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ConclusaoId }) |
                @Html.ActionLink("Details", "Details", new { id=item.ConclusaoId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ConclusaoId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
